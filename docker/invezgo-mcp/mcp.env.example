# Salin -> mcp.env (gitignored), lalu isi. JANGAN commit nilai asli.
# INVEZGO_API_KEY: samakan dgn /root/IDX_Trecker_Wahyu/.env
# MCP_AUTH_TOKEN : generate acak kuat, mis:  openssl rand -base64 32
INVEZGO_API_KEY=
MCP_AUTH_TOKEN=
